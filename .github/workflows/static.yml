name: Deploy static site to Pages

on:
  push:
    branches: ["main"]

permissions:
  contents: read

jobs:
  noop:
    runs-on: ubuntu-latest
    steps:
      - name: No deployment needed
        run: echo "Deployment handled by GitHub Pages branch source."
